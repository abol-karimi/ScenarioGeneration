import experiments.ISSTA.baselines_vs_PCGF_per_fuzz_input as baselines_vs_PCGF_per_fuzz_input
import experiments.ISSTA.baselines_vs_PCGF_per_time as baselines_vs_PCGF_per_time
import experiments.ISSTA.baselines_vs_PCGF_predicate_distribution as baselines_vs_PCGF_predicate_distribution
import experiments.ISSTA.baselines_vs_PCGF_valid as baselines_vs_PCGF_valid
import experiments.ISSTA.baselines_vs_PCGF_violations_per_fuzz_input as baselines_vs_PCGF_violations_per_fuzz_input
import experiments.ISSTA.baselines_vs_PCGF_violations_per_time as baselines_vs_PCGF_violations_per_time
import experiments.ISSTA.target_vs_surrogate_violations_per_fuzz_input as target_vs_surrogate_violations_per_fuzz_input
import experiments.ISSTA.target_vs_surrogate_violations_per_time as target_vs_surrogate_violations_per_time

baseline_plots = [
  baselines_vs_PCGF_per_fuzz_input.plot,
  baselines_vs_PCGF_per_time.plot,
  baselines_vs_PCGF_predicate_distribution.plot,
  baselines_vs_PCGF_valid.plot,
  baselines_vs_PCGF_violations_per_fuzz_input.plot,
  baselines_vs_PCGF_violations_per_time.plot,  
]

surrogate_plots = [
  # target_vs_surrogate_violations_per_fuzz_input.plot,
  target_vs_surrogate_violations_per_time.plot,
]

