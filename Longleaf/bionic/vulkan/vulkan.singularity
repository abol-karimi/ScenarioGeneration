Bootstrap: docker
From: ubuntu:18.04

%runscript
    "$@"

%setup
    mkdir -p ${SINGULARITY_ROOTFS}/home/scenariogen/ScenarioGeneration

%post -c /bin/bash
    apt-get update -q
    apt-get upgrade -y

